# EIFF Control — contexto para o Claude Code

Sistema de gestão da EIFF (engenharia, fabricação e montagem de estruturas metálicas): financeiro, tesouraria,
obras, medições, equipe e produção. Construído a partir da planilha `Fluxo_de_Caixa_EIFF.xlsx` e do
`Blueprint_Funcional_EIFF_Control_v1.docx`. Idioma do produto e das conversas: português do Brasil.

## Stack e comandos

- React 18 + Vite + TypeScript; dados no Supabase (PostgreSQL) do projeto `dduobppgomqyagjviwpx`; site estático no Netlify em `https://eiffcontrol.com.br`.
- `npm run dev` (porta 5173) · `npm test` (vitest) · `npm run build` (tsc + eslint + vite) · `npm run lint`. Para testar telas sem login: `npm run dev -- --mode demo` com um `.env.demo` local contendo `VITE_SUPABASE_URL=` e `VITE_SUPABASE_ANON_KEY=` vazios (modo local com seed; arquivo não versionado).
- Migrations SQL em `supabase/migrations/NNNN_nome.sql`, numeração sequencial. Aplicar com
  `npx supabase db query --linked --project-ref dduobppgomqyagjviwpx -f supabase/migrations/NNNN_x.sql`
  (exige `npx supabase login` feito pelo usuário nesta máquina). Nunca apagar registros financeiros; usar cancelamento/estorno (o banco proíbe DELETE em `financial_entry`, `settlement` e `bank_transaction`). Correção de erro de lançamento é exclusão lógica: `excluirLancamento` marca `deleted_at`/`deleted_by`/`deletion_reason` (migration 0037), tira o título das listas (filtro "Excluídos" mostra), do caixa, do fluxo e da DRE (`incluir` = false, situação Excluído), reverte liquidações parciais, devolve aprovações pendentes e registra auditoria; `restaurarLancamento` desfaz. Não vale para Realizado (estornar antes), conciliado, pedido de compra ou medição vinculados, período fechado.
- `.env` (não versionado) precisa de `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY`; sem eles o app roda em modo local com `src/data/seed.json`.
- Publicar: `npm run build` e `npx netlify deploy --prod --dir dist` (login Netlify do usuário), ou push no GitHub quando a integração estiver ligada (`netlify.toml`).

## Arquitetura (regras que não mudam)

- `src/core/engine.ts` é a única definição dos KPIs (fluxo 13S/24M, DRE, Obra 360°, checks). As views SQL em `0002_views.sql` espelham as mesmas regras. Mudou a regra, muda nos dois e nos testes.
- `src/core/obras.ts` (serviços, medições, demandas, produção), `equipe.ts` (apontamentos), `analise.ts` (saúde da obra), `ofx.ts` (extrato), `compras.ts` (pedidos de compra, orçado × comprado por insumo), `orcamentos.ts` (custo por composição, BDI, curva ABC, conversão em serviços), `sinapi.ts` (leitura das planilhas SINAPI, formatos antigo e unificado; preço da UF com fallback de SP como faz o SINAPI; abas ISE/CSE ignoradas; código 0 de fórmulas descartado).
- Importação SINAPI por linha de comando: `node scripts/importar-sinapi.mjs --arquivo <xlsx|pasta> --uf GO --saida NNNN_nome --todos-insumos --grupos <regex> --composicoes a,b` gera migration idempotente (upsert por origem + código, itens regravados). Os arquivos da Caixa ficam fora do repositório (`D:UsuarioDownloadsSINAPI-2026-07-formato-xlsx`).
- `src/data/store.ts` concentra as regras de negócio (validações, alçadas, aprovações, liquidação, conciliação, fechamento, auditoria, permissões por papel). Toda ação passa por `registrar()` para auditoria.
- `src/data/supabase.ts` lê o banco para o formato `Dataset` e grava só as diferenças (`persistirRemoto`). Novas entidades exigem: tipo em `types.ts`, migration, mapeamento de leitura e de gravação em `supabase.ts`, e `medicoes: []`-style default em `scripts/migrate-spreadsheet.mjs`.
- Telas em `src/screens/`; componentes em `src/ui/`. Sistema visual em `src/styles.css` (tokens: `--brand` laranja EIFF #d1481c como único acento, superfícies grafite em 3 níveis, Manrope com dígitos tabulares; tema escuro padrão, claro por `data-theme="light"` com botão no topo). Ícones vetoriais em `src/ui/icons.tsx` (`Icon`, `Marca` com as três barras da identidade). Não usar emojis como ícones nem cores fora dos tokens. Hierarquia por tela: um `KpiHero` por bloco (valor grande, secundários na base, `Sparkline`/`ProgressRow` dentro), `KpiStrip` para métricas de apoio, `Empty` com ícone e título; `PrintHead` + botão Imprimir para relatório (classes `print-only`/`no-print`). Hooks do React sempre antes de qualquer `return` antecipado (o ESLint bloqueia o build se violado).
- **Sistema de movimento** (Experience 01, `src/ui/motion.tsx` com GSAP, helpers puros em `src/ui/numero.ts`, cena three.js em `src/ui/Cena3D.tsx` carregada sob demanda por `CenaEstrutura`): a animação nunca muda conteúdo, só o caminho até ele, e tudo respeita `prefers-reduced-motion`. `useRevelar` no `App` revela em cascata os blocos de primeiro nível de cada tela (e os cartões de `.hero-grid`/`.grid`/`.strip`) a cada rota; `NumeroVivo`/`Valor` fazem os KPIs (`Kpi`, `KpiHero`, secundários, `KpiStrip`) contarem até o valor formatado preservando prefixo, separadores pt-BR e sufixo (`decomporNumero`/`recomporNumero`; datas, horas e anos soltos nunca animam); `useCrescer` faz a `ProgressRow` crescer; `IndicadorNav` desliza a barra laranja até o item ativo do menu; `MicroInteracoes` põe a ondulação nos `.btn`; `useEntrada` anima o cartão do login; o Sparkline desenha o traço (`pathLength` + `@keyframes traco`). A cena 3D é o esqueleto de um galpão metálico (pórticos, terças, contraventamentos) em wireframe laranja sobre a grade grafite, que se monta segmento a segmento (`setDrawRange`) e acompanha o ponteiro; aparece no login e no carregamento remoto, DPR ≤ 2, pausa com a aba oculta, dispose ao desmontar; sem WebGL não renderiza nada. Não usar GSAP fora de `motion.tsx`; novos padrões de movimento entram lá. Testes em `src/ui/numero.test.ts`. **Experience 02**: transição compartilhada lista → detalhe sem wiring por tela (`MicroInteracoes` lembra o retângulo do cartão/linha/link clicado dentro de `.content` e `useRevelar` faz o primeiro `.kpi`/`.card` da tela seguinte nascer daquele lugar; as 16 primeiras linhas de tabela entram em cascata); Curva S (`LineChart`) desenha cada série com atraso escalonado (`viz-traco-serie`; série tracejada só aparece), `DivergingBars`/`Gauge` crescem (`viz-cresce-h`), `Bars` sobem (`viz-cresce-v`); `Money` conta na montagem em tabelas de até 30 linhas (`NumeroVivo leve`; mudanças posteriores trocam direto); **modo apresentação** do painel (`src/screens/Apresentacao.tsx`, botão Apresentar): os mesmos números do motor em tipografia grande, 4 slides (caixa, carteira, compromissos 13S, alertas), setas/espaço/clique avançam, Esc sai, avanço automático a cada 9 s, tela cheia quando permitido, cena 3D `apresentacao` ao fundo. Limpeza dos hooks é explícita (kill + clearProps): um estado "from" nunca fica no elemento. Em abas ocultas o navegador pausa o requestAnimationFrame e as animações congelam até a aba voltar (comportamento do navegador, não do app).
- **Bloco 1 de UX** (paleta, tabela, skeletons, code-split): toda tela é `React.lazy` no `App` (um chunk por tela; `Suspense` com `SkeletonTela` e o wrapper `Revelar` de `motion.tsx` para a cascata rodar quando a tela realmente monta). **Paleta de comandos** `src/ui/Paleta.tsx` (Ctrl+K / Cmd+K ou botão Buscar na barra): navega por `ROTAS_NAV` (manter alinhada à sidebar do App, respeitando `permissao`), executa ações do App (tema, menu, densidade, imprimir, recarregar, sair) e busca obras, lançamentos, empresas e contatos do Radar e orçamentos; busca pura em `src/ui/busca.ts` (`normalizar`, `pontuar`, `buscar`); recentes em localStorage. **Tabela unificada** `src/ui/Tabela.tsx`: `colunas` (título, `num`, `ordenar`, `oculta`) + `linhas` + `linha(item)` devolvendo os `<td>` (as células continuam da tela), ordenação estável por coluna (`ordenarLinhas`), cabeçalho fixo em área rolável (`altura`), linhas clicáveis navegáveis por setas/Enter, estado vazio, zebra; densidade global `data-densidade` no `<html>` (`lerDensidade`/`aplicarDensidade`, alternada pela paleta). Migradas: Lançamentos e Radar · Empresas; novas tabelas devem usar `Tabela`. `Skeleton`/`SkeletonTela` em `components.tsx`. Testes em `src/ui/busca.test.ts`.
- **Bloco 2 de UX** (fundação de design): a sidebar é gerada de `ROTAS_NAV` (mesma lista da paleta; contagens por rota em `contagens` no `App`), com grupos recolhíveis (`h3.grupo`, estado em localStorage `eiff-control:grupos`; o grupo da tela ativa nunca fica fechado) e favoritos (estrela no hover, seção "Favoritos" no topo, `eiff-control:favoritos`). Tokens de espaçamento e tipografia em `:root` (`--space-1..8`, `--text-xs..2xl`) usados por body, h1-h3, `.small`, `.card`; escala de texto `data-escala="grande"` alternada pela paleta (`eiff-control:escala`). Formulários: `Field` aceita `erro` (mensagem inline com `role="alert"`, campo em vermelho) e `erroDoCampo(erros, /regex/)` casa as mensagens do store com o campo — `LancamentoForm` usa isso para categoria, obra, contraparte, descrição, competência, vencimento, probabilidade, conta e valores; `MoedaInput` (pt-BR, `parseMoeda`/`formatarMoedaEntrada` em `numero.ts`) substitui `NumberInput` nos valores monetários. Estados: `EstadoErro` (título, causa, orientação, ações; usado na falha de carga inicial) e `Empty` com marca d'água da EIFF e `acao` opcional. Tema claro em paridade: overlays, sombras, cena 3D, skeleton e zebra têm override em `[data-theme="light"]`.
- **Bloco 3 de UX** (obra, fábrica e caixa): **Cronograma visual** na Obra 360 (aba "Cronograma visual", `src/ui/Gantt.tsx`): Gantt dos serviços com barra prevista (início/fim previstos) preenchida pelo `pctExecucao` do motor, linha real (início/fim reais ou até hoje), marcos de medição como losangos (medida/atrasada/pendente), meses e "hoje"; abaixo, curva S financeira (previsto acumulado × faturado acumulado, líquido da construtora) a partir de `o.medicoes.porMes`; sem datas nos serviços mostra orientação. **Estrutura 3D do avanço** na aba Materiais (`src/ui/CenaObra.tsx`, sob demanda, botão "Ver estrutura 3D do avanço"): disposição ESQUEMÁTICA (a lista não tem coordenadas) de pilares, vigas/treliças, terças, contraventamentos e demais conjuntos, com a cor do estado real (`CORES_SITUACAO`: não liberado, liberado, em fabricação, fabricado, expedido, montado) e tamanho pelo peso; legenda com contagem e kg por estado; respeita os filtros da aba. **Modo quiosque** da fábrica (`src/screens/Quiosque.tsx`, botão em Fábrica e montagem ou `#/producao?quiosque=1` para a TV): estações de fábrica e canteiro com kg, HH, peças e kg/HH do dia contra `META_KG_HH`, totais fabricado/expedido/montado, 14 dias em sparkline, relógio; lê `resumoProdutividade` (nada calculado ali); Esc sai. **Cenários interativos de caixa** no Fluxo 13 semanas (`src/core/cenarioCaixa.ts` + `CenarioInterativo` em `Tesouraria.tsx`): atraso de recebimentos, adiamento de pagamentos, corte em despesas sem obra e novo contrato (valor, primeira parcela, parcelas) aplicados a uma cópia dos lançamentos previstos (`aplicarCenario`; realizados, cancelados e excluídos não mudam; nada é gravado) e recalculados por `fluxo13Semanas`; KPIs do cenário com delta contra a base e gráfico base × cenário × reserva. Testes em `src/core/cenarioCaixa.test.ts`.
- **Bloco 4 de UX — modo campo offline (PWA)**: `public/sw.js` (registrado em produção por `main.tsx`) guarda a casca do app — assets com hash cache-first, navegação/index network-first com fallback, fontes stale-while-revalidate; Supabase, `/api/*` e qualquer POST nunca passam pelo service worker. **Fila offline no store**: `sincronizar` classifica a falha com `ehErroDeRede` (`src/data/rede.ts`): erro de rede → `sync.status = 'pendente'`, alterações guardadas no IndexedDB (`src/data/offline.ts`, chave `pendente`: usuário, **base que o usuário viu** e dataset atual) e reenvio automático ao voltar a rede (`online`) ou a cada 30 s; erro de regra/conflito → `erro` como antes. Ao abrir: `carregarComPendentes` carrega o remoto, aplica a pendência como `persistirRemoto(base, ds)` (só o que o usuário mudou; conflitos de versão continuam bloqueando) e recarrega; sem rede, abre com o último dataset em cache (`cache`) mais a pendência, em modo `pendente`; se a pendência não puder ser aplicada, ela fica na tela com status `erro` e o usuário escolhe "Tentar de novo" ou "Descartar pendências" (`actions.descartarPendencias`). `tentarNovamente` recarrega primeiro quando o provedor não chegou a carregar (`remotoPronto`). **Fotos de campo** (`Dataset.fotos`, tabela `field_photo`, migration 0041): `BotaoFoto` no modo campo (ordens, tarefas e check-list) abre a câmera, `comprimirFoto` (`src/ui/foto.ts`) reduz a 1280 px JPEG ≤ 800 KB em data URL, `actions.registrarFoto`/`excluirFoto` (autor ou quem edita a obra) com auditoria; miniaturas no cartão. A imagem vai na própria linha para sincronizar pelo mesmo diff da fila offline — `carregarRemoto` lê todas as fotos, então vale mover para Storage quando o volume crescer. Testes: `src/data/rede.test.ts`, `src/data/fotos.store.test.ts`.
- **Bloco 5 de UX** (conciliação por arrastar, vistas salvas e exportação, tour guiado): em Bancos e conciliação, o cartão **Pares sugeridos** lista, para cada transação pendente, a melhor sugestão de `sugerirConciliacao` com score ≥ 75 dentro da tolerância (score ≥ 90 = "seguro"; botão concilia todos os seguros, um lançamento nunca é usado duas vezes na mesma rodada), e as transações pendentes são arrastáveis: soltar sobre um lançamento em aberto do mesmo tipo (lista ordenada pela diferença) concilia dentro da tolerância ou abre a seleção com o lançamento marcado para justificar; a regra continua toda em `actions.conciliar`. **Vistas salvas** (`src/ui/vistas.tsx`, `VistasSalvas`): filtros com nome por tela e navegador (`eiff-control:vistas:<tela>`), em Lançamentos (por modo) e Radar · Empresas. **Exportação CSV** (`src/ui/exportar.ts`, pt-BR com BOM e ";"): prop `csv` da `Tabela` exporta o que está na tela (filtro e ordenação) — Radar · Empresas já usa; Lançamentos mantém o botão próprio. **Tour guiado** (`src/ui/Tour.tsx`): passos por rota em `POR_ROTA` mais os genéricos por estrutura (`GERAL`: cabeçalho, indicadores, faixa, filtros, abas, tabela, busca, menu), só os cujo seletor existe na tela; a lição da Capacitação da mesma rota vira o primeiro passo com "Ver lição"; abre sozinho na primeira visita a cada tela (`eiff-control:tour:<rota>`, nunca no modo campo), pelo "?" da barra ou pela ação "Tour desta tela" da paleta; setas e Esc. Ao criar uma tela nova, acrescentar passos em `POR_ROTA` se a estrutura genérica não bastar.
- **Bloco 6 de UX** (assistente contextual, curva S de custo, telemetria local): **sugestões por tela** em `src/core/sugestoes.ts` (`sugestoesPara(rota, ds, usuario, hoje)`, função pura sobre o motor: painel — realizados sem conciliação, SLA de aprovação vencido, obras sem datas nos serviços; lançamentos/pagar/receber — custo direto sem obra, recebíveis vencidos > 30 dias, pagamentos vencidos; obra 360 — serviços sem datas, medições atrasadas, margem negativa, faturamento direto estourado, serviços em andamento sem avanço; radar — próximas ações vencidas, contas A/A+ sem decisor, duplicatas pendentes; conciliação — pendentes > 7 dias, divergentes; produção — ordens sem data de necessidade; aprovações — SLA vencido); `src/ui/Sugestoes.tsx` mostra até 4 como faixa no topo do conteúdo (dentro do `Revelar`), com ação que navega e "×" que dispensa na sessão. Regra nova de sugestão entra em `sugestoesPara` (com teste em `sugestoes.test.ts`), nunca na tela. **Curva S de custo** no cronograma da Obra 360 (`Gantt` recebe `lancamentos` = movimentos da obra): comprometido acumulado por competência × pago acumulado por data de caixa × custo previsto dos serviços, acima da curva financeira. **Telemetria de uso** (`src/data/telemetria.ts`): local e anônima, só no navegador (localStorage, anel de 3000 eventos) — visitas por tela (`registrarVisita` no App), ações (`registrarAcao`: paleta, tour, exportações, sugestões); resumo de 30 dias no cartão "Uso do sistema neste navegador" da Auditoria, com "limpar". Nada é enviado a servidor.
- **Modo campo 2** (lançamento de campo pelo celular, `src/screens/Campo.tsx` + `CampoDiario.tsx` + `CampoEstacao.tsx`): a tela inicial mostra o dia escolhido (Hoje, Ontem ou data até 7 dias atrás, guardado na sessão em `eiff-control:campo:data`), a faixa do dia (`resumoDiaCampo` em `equipe.ts`: diários fechados, presentes, horas, toneladas apontadas) e três blocos: **Equipe do dia** (um botão por local vindo de `locaisDoDia`, agora pela alocação vigente — `equipeDoLocal` — com fallback no cadastro), **Produção por estação** (Fábrica e Montagem no canteiro) e Acompanhamento (ordens, tarefas, check-list, painel). `/campo/diario?local=&obra=` é o diário no celular: presença por segmento P/F/At/Fé/Fo, horas e extras em passos de 0,5 h (`Stepper`), serviço/observação por pessoa, "Adicionar pessoa de outra equipe" (marcada como emprestada), ocorrências por chips com horas perdidas, produção opcional, clima (só obra), observações e fotos (`referenciaTipo` `apontamento`, após guardar), rodapé fixo com efetivo/horas/custo e Guardar / Fechar o dia / Reabrir com motivo — tudo por `actions.salvarApontamento`/`reabrirApontamento` (mesmas regras do diário completo em `/apontamentos`). `/campo/fabrica` e `/campo/montagem` apontam estação (`actions.apontarEstacao`): obra por chip (pré-escolhida quando só há uma), estação por chip, quilos em campo grande, peças, conjuntos da lista por busca com marco fabricado/expedido/montado, ordem opcional, equipe do local já listada e marcada com as horas do diário do dia (`apontamentoDoDia`), kg/HH calculado antes de registrar, e a lista do que já foi registrado no dia. `novoApontamento` no store passou a usar `equipeDoLocal`/`linhasPadrao`. Sem migration. Testes em `src/core/campo.test.ts`.
- **Fluxo guiado do dia** (modo campo, `src/core/campoFluxo.ts` + `src/screens/CampoFluxo.tsx`): a home do modo campo tem o bloco **Preencher o dia** com uma frente por botão — Fábrica e um Canteiro por obra em execução visível (`estadoDoFluxo`: não iniciado / continuar / fechado com presentes, ausentes, extras e kg). `/campo/fabrica` e `/campo/canteiro?obra=` abrem o assistente de 6 passos (`passosDoFluxo`: O dia, Faltas, Hora extra, Produção, Ocorrências, Fechar), um por pergunta, com barra de passos clicável e rodapé Voltar/Próximo. Regra de exceção: todos os alocados nascem presentes na jornada (`iniciarRascunho`/`linhasPadrao`); no passo Faltas toca-se só em quem não veio e escolhe-se o motivo (`alternarAusencia`, `AUSENCIAS`), no passo Hora extra só em quem fez (ou "Ninguém fez"), no passo Ocorrências chips por tipo (ou "Nenhuma"). Clima e temperatura viajam juntos no campo `clima` do diário ("Nublado · 31 °C", `climaTexto`/`climaDe`; canteiro exige clima, fábrica pergunta turno). Produção: quilos e peças por estação da linha (`marcacoesVazias`) e quem trabalhou nela; ao fechar, `horasPorEstacao` divide as horas do diário de cada pessoa igualmente entre as estações em que foi marcada (nunca conta a mesma hora duas vezes; estação sem kg/peças é ignorada) e cada estação vira um `apontarEstacao` com observação "fluxo do dia (celular)". O rascunho (`RascunhoFluxo`) fica em `localStorage` `eiff-control:campo:fluxo:<data>:<frente>:<obra>` até fechar; "Guardar" grava o diário como rascunho no sistema. `pendenciasDoFluxo` bloqueia o Fechar até as confirmações (clima no canteiro, hora extra e ocorrência confirmadas, estação com produção tem gente, "Outra" descrita). Dia fechado abre em modo leitura com "Registrar outra estação" e "Reabrir para corrigir" (motivo, `reabrirApontamento`). Registro avulso: `/campo/estacao?linha=` (CampoEstacao) e `/campo/dia` → `/campo/diario` (diário completo por local). Testes em `src/core/campoFluxo.test.ts`.
- **Bloco 7 de UX** (fotos no Storage, acessibilidade): a foto continua nascendo comprimida no aparelho (`dataUrl`), mas ao sincronizar `persistirRemoto` sobe o arquivo para o bucket `fotos-campo` (`<organization_id>/<project_id|sem-obra>/<id>.jpg`) e grava só `storage_path`; se o upload falhar sem rede, o erro sobe e a fila offline guarda a foto com a imagem local até a próxima tentativa. `Foto.dataUrl` ficou opcional e `Foto.caminho` guarda o objeto; `urlFoto(f)` (`supabase.ts`) devolve a imagem local ou uma URL assinada de 1 h com cache; `ImagemFoto` no modo campo resolve a miniatura e mostra um marcador quando não há rede nem imagem local; excluir a foto remove o objeto. Fotos antigas com `data_url` na linha continuam válidas. Acessibilidade: `Tabs` com `role="tablist"`/`aria-selected` e setas; toast em região `role="status" aria-live="polite"`; `Modal` foca o primeiro campo, prende o Tab e devolve o foco ao fechar; link "Ir para o conteúdo" (`.salto`) e `main#conteudo` focável; menu com `aria-label`; `--muted` do tema claro subiu para 4,9:1 de contraste.
- Testes comparam o motor com a planilha centavo a centavo (`engine.test.ts`); não altere valores esperados sem justificar.
- RLS: política de SELECT nunca deve consultar a própria tabela por função STABLE (ex.: `can_access_project(id)` em `project`), porque `INSERT ... RETURNING` não enxerga a linha nova e falha com "new row violates row-level security policy". Use colunas da própria linha e helpers que leem só `user_scope` (migration 0025).

## Regras de negócio essenciais

- Valores positivos; o tipo (Entrada/Saída) dá o sinal. Caixa usa data de realização ou vencimento; DRE usa competência.
- Saída acima de `LIMITE_GESTOR_OBRA`, fora do orçamento ou que aprofunde o caixa abaixo da reserva exige aprovação Gestor → Financeiro → Diretoria; solicitante não aprova o próprio pedido (Administrador é a exceção na fase de validação).
- **Corte do extrato** (`Params.corteExtrato`, coluna `parameter_set.statement_cutoff`, campo "Corte do extrato (OFX)" em Cadastros › Parâmetros; produção = 2026-09-01 por decisão da Diretoria em 10/09/2026): movimentos do extrato com data anterior ao corte ficam fora da posição bancária (`posicaoBancaria`: início = max(abertura da conta, corte)), da conciliação (`calcTransacoes`) e das importações OFX/CSV (`importarTransacoes` descarta e devolve `antesDoCorte`), inclusive os já importados e pendentes; nada é apagado (o banco proíbe DELETE em `bank_transaction`). `incluirTransacao(t, params)` é o filtro único. A Posição diária avisa quando uma conta tem saldo de abertura anterior ao corte (a abertura deve ser o saldo do extrato no dia do corte) e a Conciliação mostra quantas transações ficaram fora. Testes em `src/core/corteExtrato.test.ts`.
- Movimento bancário é fato: "Lançar a partir da transação" cria lançamento realizado, liquidado e conciliado, sem alçada.
- Conta financeira do título: a liquidação total leva o título para a conta da liquidação (o caixa por conta segue o banco em que o dinheiro entrou); `alterarContaLancamento` (botão Alterar conta no detalhe) troca a conta em qualquer status não cancelado (Realizado exige alçada de liquidar), move as liquidações junto, registra auditoria e é bloqueada quando há transação do extrato vinculada. O "saldo bancário hoje" do painel e da Tesouraria vem só do extrato; liquidações aparecem em "saldo por lançamentos" e no fluxo.
- Serviços da obra vêm do cronograma físico-financeiro do contrato. Custo previsto do serviço, nesta ordem: custo orçado próprio; custo direto dos itens do orçamento executivo Contratado vinculados ao serviço (`custoOrcamentoPorServico`); receita × (1 − margem alvo). O orçamento disponível da obra = custo previsto − comprometido. EAC = pago + comprometido em aberto + ETC não comprometido.
- Faturamento direto ao cliente (`Lancamento.faturamentoDireto` / `financial_entry.direct_billing`): compra da obra que o cliente paga direto ao fornecedor. Só para saídas com obra. Não entra no caixa, no fluxo, no DRE nem no aging da EIFF (valorCaixaProjetado e valorGerencial = 0), mas conta no comprometido do serviço/obra e abate o saldo de faturamento direto do contrato (soma de `medicoes.faturamentoDireto`, ou dos serviços). Views SQL espelham em 0023 (`direct_billing_*` em `v_project_360`, `v_service_cost`).
- Avanço físico do serviço (`calcServico.origemExecucao`), nesta ordem: Concluído; fabricação × montagem ponderadas pela lista de materiais em kg; idem pelas ordens de produção (`producaoPorServico`); medições físicas do serviço (`AvancoServico`, tabela `service_progress`, migration 0027, botão Medir na aba Serviços: quantidade ou %, acumula, não passa do total, primeira medição põe o serviço Em andamento); quantidade executada informada; senão % faturado do cronograma. Peso da fabricação por serviço em `pesoFabricacao` (`fab_weight`, padrão 0,6).
- Lista de materiais (`Conjunto`, tabela `assembly`, migration 0026, `src/core/materiais.ts`, aba Materiais na Obra 360): conjuntos/marcas em kg por obra e serviço, importados de Tekla/SolidWorks/Excel (colunas reconhecidas pelo cabeçalho) ou colados; apontamento em lote de liberado/fabricado/expedido/montado (acumula, limitado ao total, montado puxa expedido puxa fabricado). Quando o serviço tem lista, o avanço físico passa a ser peso montado ÷ peso total (`origemExecucao`), alimentando Obra 360, painel e curva S.
- Fábrica e montagem (`#/producao`, `src/core/producao.ts`, tela `Producao.tsx`, migration 0028: `station_log`, `shipment`, `v_station_productivity`): apontamento diário por estação (fábrica: Corte, Furação, Montagem e ponteamento, Solda, Pintura, Expedição; canteiro: Recebimento em obra, Pré-montagem, Içamento, Fixação / torqueamento, Liberação) com kg, peças, conjuntos e horas por colaborador. Pintura marca os conjuntos como fabricados, Expedição/romaneio como expedidos, Liberação como montados; a ordem informada acumula a quantidade na etapa de mesmo nome (`ETAPAS_FABRICACAO` agora são as estações; `ETAPA_LEGADA` traduz nomes antigos). Produtividade em kg/HH por estação, colaborador e dia contra `META_KG_HH` (17,5 HH/t fábrica, 26 HH/t campo, das composições EIFF-FAB-KG/EIFF-MON-KG); custo de mão de obra = horas × custo/hora do colaborador. Romaneio (`ROM-nnnn`) lista conjuntos por carga, não ultrapassa o total, Entregue/Cancelado (cancelar devolve o expedido).
- Estoque de aço (`#/estoque`, `src/core/estoque.ts`, tela `Estoque.tsx`, migration 0029: `stock_item`, `stock_movement`, views `v_stock_position`, `v_heat_traceability`, `v_steel_cost_by_service`): itens por família (perfil, chapa, tubo, consumível…), tudo em kg. Movimentos imutáveis: Entrada (corrida obrigatória para aço estrutural; pedido de compra preenche fornecedor/NF/kg/preço), Consumo (exige obra; lote/corrida com saldo no local; recebe o custo médio do lote; pode citar serviço, ordem e conjuntos cortados), Sobra (retalho volta ao lote), Ajuste (quantidade com sinal, justificativa) e Estorno (movimento inverso; entrada já consumida não estorna). Saldo por item/lote/local e custo médio móvel em `posicaoEstoque`; custo real de aço por obra/serviço em `consumoAco` (exposto em `obra360.aco` e na faixa da Obra 360); rastreabilidade corrida → obras/conjuntos e conjunto → corridas.
- Capacitação (`#/capacitacao`, `src/core/capacitacao.ts`, tela `Capacitacao.tsx`, migration 0030 `training_progress`): conteúdo de estudo em código (lições com passos, campos obrigatórios, regras automáticas, erros comuns e verificação; trilhas por papel em `TRILHAS`; processos ponta a ponta em `PROCESSOS`; rotinas diária/semanal/mensal em `ROTINAS`). Concluir lição exige acertar a verificação (`concluirLicao`); progresso por usuário; gestores veem a equipe; "Imprimir manual" imprime a trilha do papel. **Ao mudar uma regra de negócio, revisar a lição correspondente** (o teste `capacitacao.test.ts` valida rotas e referências, não o texto).
- Assistente e e-books (`docs/assistente.md`): botão flutuante de chat em todas as telas (`src/ui/Assistente.tsx`); em produção chama `/api/assistente` (`netlify/functions/assistente.ts`, bundler esbuild) que valida o token do Supabase Auth, lê o papel do perfil e consulta a API da Anthropic com o conhecimento de `src/core/assistente.ts` (regras + lições + processos + rotinas + setores, com cache de prompt). Exige `ANTHROPIC_API_KEY` no painel do Netlify (nunca no repositório); sem ela ou em modo local, responde por busca no manual (`buscarLocal`). E-books por setor e completo em `src/core/ebook.ts` (HTML autocontido gerado no navegador, Capacitação › E-books; "Salvar como PDF" para distribuir).
- EIFF Radar (`#/radar`, `#/radar/hoje`, `#/radar/empresas[/:id]`; `src/core/radar/*`, telas `src/screens/radar/*`, persistência `src/data/radar.supabase.ts`, migration 0031 com tabelas `radar_*`; guia em `docs/radar.md`): inteligência comercial + CRM. Empresa é a raiz (Lead não é entidade). Score por regras configuráveis (`radar_score_rule`, dimensões FIT/TIMING/INTENT/RELATIONSHIP/DATA_QUALITY, decaimento linear, pesos e cortes em `radar_score_setting`, snapshots com explicação); nada de peso fixo no código, padrões em `padroes.ts` semeados na primeira carga (`garantirPadroesRadar`). Regra crítica: oportunidade ativa sem próxima ação é recusada no store e sinalizada nas telas. Dedup CNPJ → domínio → nome+local → parecido (possível duplicata para revisão). Importação CSV com job/linhas/erros e registro bruto (`radar_source_record`). Adapters em `adapters.ts` normalizam fontes externas antes de persistir. Permissões `radar` e `radar_config`. `Dataset.radar` é um slice aninhado (`RadarDataset`). Decisores (`src/core/radar/contatos.ts`): persona por regras configuráveis (`radar_persona_rule`), decision fit 0-100 com matriz em `radar_decision_fit_weight`, contato principal sugerido com razões (nunca do_not_contact/INVALIDO/SAIU_DA_EMPRESA), qualidade de contato, estados de próxima ação (SEARCH_DECISION_MAKER, ENRICH_CONTACT, RESEARCH_SIGNALS, CONTACT_NOW); importação de contatos associa por CNPJ → business_id da Explorium (32 hex, coluna `business_id`) → id externo → domínio → nome e manda ambiguidade para a fila de revisão sem criar empresa; a lista de empresas do Vibe grava `business_id` (`CAMPOS_EMPRESA.businessId`) e os níveis da Explorium viram a escala de senioridade do Radar (`senioridadeDe`). **Dry run** antes de importar contatos: botão "Dry run" no importador (`dryRunContatosCsv` em `src/core/radar/dryrun.ts`, nada gravado) ou `npm run radar:dry-run -- --contatos <csv> [--empresas <csv>] [--total 91] [--detalhes]`. **Carga em produção** sem sessão do navegador: `npx vite-node scripts/radar-importar-producao.mts -- --empresas <csv> --contatos <csv> --perfil <uuid> --esperado <json> [--executar]` usa `importarCsv`/`recalcularEmpresas` (core, `src/core/radar/importacao.ts`, os mesmos do store) e o mapeamento de `radar.supabase.ts` (`linhaDb`) para gerar SQL aplicado pelo Supabase CLI numa transação; sem `--executar` só simula e confere os números esperados. **Signal Pilot 01** (aba no Command Center, `src/core/radar/signalPilot.ts`, tela `SignalPilot.tsx`): 10 empresas fixas; leitura comercial do sinal (relevância estrutural DIRECT/INDIRECT/CONTEXTUAL/NONE, o que aconteceu, por que importa, ação) informada no formulário Registrar sinal e guardada em `raw_payload.leitura` ao lado do bruto (`payloadComLeitura`/`leituraDe`; sem migration; sinais são insert-only, então a leitura entra no registro); relevância derivada do tipo quando não informada (`RELEVANCIA_PADRAO_POR_TIPO`); grupos A/B/C por tipo só para exibição/ordenação; faixas de confiança 90/70/40; matriz operacional `recomendacaoSignalPilot` (HOT/WARM/NO_EVIDENCE → CONTACT_NOW/FIND_BETTER_DECISION_MAKER/RESEARCH_PROJECT/WATCH/RESEARCH_SIGNALS/NURTURE/IGNORE) com limites em `HIPOTESE_SIGNAL_PILOT` (hipótese do piloto, não regra do CRM; a ação do analista prevalece); WHY NOW só com dados existentes; Signal Intelligence Report 01 ordenado por ação, priority, timing e confiança. Nada disso altera score, pesos ou decision fit. **Intelligence economics** no Command Center (`src/core/radar/economia.ts`): créditos consumidos, empresas pesquisadas, prospects, e-mails válidos, contas cobertas e razões, lidos do ledger `radar_vibe_operation` (carregado como `ds.radar.operacoesVibe`, somente leitura); sem consumo mostra 0 e "—".
- Radar — Production Calibration 01 (08/09/2026, aplicada em produção por `scripts/radar-calibracao-aplicar.mts`, simulação antes e transação única): FIT balanceado como regras de configuração `fitCalibrado` (Geografia 15, Setor 35, Funcionários 25, Receita 20, Porte industrial 5; regras FIT antigas desativadas, não apagadas); setor por categoria canônica (`fitCalibracao.ts`: `classificarSetor` com confiança HIGH/MEDIUM/LOW e gate 100%/70%/0 — LOW nunca pontua setor e pede revisão; dado original nunca reescrito, faixas com colchetes lidas sem alterar); decaimento por família (`sinalLeitura.ts`: LONG 540 / MEDIUM 270 / SHORT 120; NEW_OFFICE e FUNDING seguem em 180) e recência do Signal Pilot pela mesma família (fallback 120); fonte `OFFICIAL_COMPANY_SOURCE` (0,95) para comunicados oficiais, `WEBSITE` segue 0,6; CRM: sinal acionável (grupo A/B, relevância DIRECT/INDIRECT, confiança ≥ 0,40) + decision fit ≥ `fit.ideal` → CONTACT_NOW, abaixo → SEARCH_DECISION_MAKER, sem sinal acionável vale a regra antiga. Não mudar cortes de classe nem pesos das dimensões sem nova calibração aprovada. Novos sinais reais: `scripts/radar-registrar-sinal-producao.mts`.
- Radar — Communication Intelligence 01 (`src/core/radar/comunicacao.ts`, `comunicacaoGeracao.ts`, painel `src/screens/radar/Abordagem.tsx` na página da empresa e na fila Hoje): motor de contexto de comunicação. Separa FATOS (com fonte, `verificado`, confiança, data e URL; só fato verificado entra em `fatosPermitidos`; a leitura "por que importa" do analista nunca é fato), OBJETIVO (catálogo `OBJETIVOS`: GET_REFERRAL, START_DISCOVERY, UNDERSTAND_PROJECT_STAGE, QUALIFY_NEED, REQUEST_PROJECT, OFFER_PRELIMINARY_STUDY, SEND_REQUESTED_CONTENT, SCHEDULE_MEETING, FOLLOW_UP, REACTIVATE, PROCUREMENT_ROUTING, cada um com condição de sucesso, CTA, estágios e personas), PLAYBOOK (`PLAYBOOKS`: regras fazer/não fazer, tom, elementos obrigatórios/proibidos, objeções; nunca texto fechado), CANAL (`recomendarCanal`: persona/estágio/histórico, rotação após sem resposta, REFERRAL como secundário), CONTENT SPEC (`montarContentSpec`, entrada futura do LLM), GERAÇÃO (`generateCommunication` com interface de provedor; hoje só o provedor determinístico, sem chave de IA e sem chamada client-side; o provedor LLM entrará por função Netlify protegida), APROVAÇÃO (estados DRAFT → READY_FOR_REVIEW → APPROVED/REJECTED; SENT/REPLIED bloqueados no store nesta fase; a geração nunca passa de READY_FOR_REVIEW) e RESULTADO (`TRANSICOES_RESULTADO` mapeia cada código de `radar_response_type` ao próximo objetivo/playbook; NO_PROJECT, INVALID_CONTACT, NOT_INTERESTED, ALREADY_HAS_SUPPLIER, COMPETITOR_SELECTED e NEGATIVE não geram comunicação). `selecionarPlaybook`: último resultado → indicação → compras → persona técnica/operacional → executivo (fit ≥ ideal decide; abaixo, GET_REFERRAL via ACCESS_VIA_EXECUTIVE). Rascunhos em `ds.radar.comunicacoes`, persistidos em `radar_communication` (migration 0038, spec `comunicacoes` em `radar.supabase.ts`): `content_spec` é o snapshot mínimo (`contentSpecPersistivel`: claims permitidos com id/fonte/verificação, objetivo, playbook, canal, CTA, versões; nunca raw_payload, telefone, e-mail ou LinkedIn, com CHECK no banco), `generated_content` original preservado e `edited_content` com editor e data; `context_hash` é SHA-256 canônico (`hash.ts`) com índice único parcial por rascunho ativo (idempotência: mesmo contexto devolve o rascunho existente); invariantes no core (`validarTransicaoComunicacao`) e no banco (CHECKs + trigger `radar_communication_state_event`, que também grava todo estado em `radar_communication_event` com ator e motivo): SENT só com `sent_activity_id` (atividade manual do contato, nunca NOTE), REPLIED só com envio + atividade com resultado, READY_FOR_REVIEW nunca vai direto a SENT; APPROVED revalida o conteúdo efetivo (edição humana passa pelo fact gate). RLS igual às demais tabelas radar_* (papéis da permissão `radar`). O tipo interno do sinal nunca vai ao prospect: `referenciaPublicaDoSinal`/`REFERENCIA_PUBLICA_SINAL` (teste varre texto, assunto, roteiro, alternativas e objeções). Nenhum nome de conta real no motor (teste garante). Não enviar nada. Hardening 01: cada claim tem id, tipo (FACT / INTERPRETATION / INTERNAL_REASONING / TECHNICAL_CLAIM) e regime de divulgação (ALLOWED / INTERNAL_ONLY; PARTNER e indicação humana são INTERNAL_ONLY até autorização explícita `citarIndicacao`); o provedor recebe só `allowedClaims` (nunca raw_payload); `referenciaAoSinal` fala do sinal na linguagem da fonte real (CNO = registro de obra, PNCP = contratação/plano, fonte oficial = comunicado, WEBSITE = publicação da empresa, NEWS = notícia, PARTNER/MANUAL/CSV/VIBE = "movimento relacionado a"), sem expor códigos; claims técnicos só se `aprovado` (`CLAIMS_TECNICOS`: avaliação preliminar e insumos do estudo sim; peso/prazo, economia, preço não); `validarGeracao` é o fact gate pós-geração (números, datas e entidades só de claims permitidos, elementos obrigatórios/proibidos, CTA do objetivo, sem presunção de projeto aberto ou de responsabilidade do contato, sem promessa não autorizada, sem revelar fonte confidencial); saudação pelo horário local ou "Olá, <nome>"; idempotência por `contextHash`; versões (`PLAYBOOK_VERSION`, `CONTENT_SPEC_VERSION`, `PROMPT_VERSION`, provedor, modelo) gravadas no rascunho. **LLM Provider 01**: função Netlify `/api/comunicacao` (`netlify/functions/comunicacao.ts`, SDK `@anthropic-ai/sdk`, chave `ANTHROPIC_API_KEY` só no painel; modelo `ANTHROPIC_COMMUNICATION_MODEL` → `claude-sonnet-5`, nunca herda `ANTHROPIC_MODEL` do Assistente; juiz `ANTHROPIC_COMMUNICATION_JUDGE_MODEL` → `claude-sonnet-5`) com handler puro e testável em `src/core/radar/comunicacaoServidor.ts` (fetch e portas do LLM injetados): JWT → usuário → papel só do perfil do banco (sem perfil = 403; papel fora de `PAPEIS_RADAR` = 403; nada vindo do cliente) → `validarRequisicaoGeracao` (`comunicacaoLlm.ts`: catálogos fechados de objetivo/playbook/canal, chaves proibidas em qualquer nível, claims só FACT/TECHNICAL_CLAIM verificados, CTA do catálogo, `contextHash` recalculado no servidor) → idempotência por hash ativo (devolve a existente sem tokens) → `orquestrarGeracaoLlm` (prompt `COMMUNICATION_LLM_PROMPT_V1`, saída JSON por `output_config.format` json_schema, `claims_used` só ids existentes, fact gate determinístico + juiz semântico `PROMPT_JUIZ_V1`, uma regeneração corretiva; falha = 422, nada persistido) → INSERT completo em `radar_communication` (READY_FOR_REVIEW, provider ANTHROPIC, modelo, prompt_version, métricas de tokens/latência/regeneração em `generated_content.metadados`) com o JWT do usuário (RLS). O app incorpora a linha por `incorporarComunicacaoRadar` (registra a ref para não reinserir). UI: "Gerar com IA" por canal, "Gerar versão padrão" (provedor determinístico, fallback quando a IA está indisponível), alternativas com "Usar esta", metadados discretos (provedor, modelo, prompt, validação, hash). Nenhuma chamada real à Anthropic na suíte (provedor mock). **Server Truth 01**: o contrato público de `/api/comunicacao` é só `{ empresaId, contatoId, sinalId?, estrategiaId?, canal, citarIndicacao?, horaLocal? }` (`validarPedidoGeracao`; qualquer outro campo — spec, claims, objetivo, hash, remetente — é 400); o servidor reconstrói o ContentSpec a partir do banco com o JWT do usuário (`comunicacaoServidor.ts`: empresa, contato, sinal, estratégia, atividades, contatos, fontes, pesos do decision fit) via `contextoComunicacaoDe` + `montarContentSpec`, valida relações (contato/sinal da empresa, organização, estratégia ativa: 400/403/404), canal só entre os disponíveis do contato (400 `canal_indisponivel`), remetente do perfil + tabela `company` + `EIFF_REMETENTE_CIDADE` (nunca do cliente), `citarIndicacao` só libera a fonte com indicação real associada ao contato (padrão INTERNAL_ONLY), `contextHash` só server-side e idempotência depois da reconstrução; migration 0040 (trigger `radar_communication_coerencia`, security definer) recusa no banco contato/sinal de outra empresa e organização divergente; prompt declara claims e referências como dados não confiáveis e `sanitizarTexto` remove controle/tags/quebras e limita tamanho e quantidade sem alterar fatos. **Latency Budget 01** (o teste real devolveu 504 do Netlify: função síncrona tem 60 s rígidos e o cliente usava timeout 60 s + retry do SDK + gerador e juiz sequenciais com effort medium): `configuracaoLlm(env)` em `comunicacaoLlm.ts` fixa `effort: low`, `MAX_TOKENS_GERACAO` 1200, `MAX_TOKENS_JUIZ` 350, `OPCOES_CLIENTE_ANTHROPIC` com `maxRetries: 0`, timeout por chamada `ANTHROPIC_CALL_TIMEOUT_MS` (22 s) e deadline total `COMMUNICATION_DEADLINE_MS` (50 s, reserva ~10 s para Supabase/INSERT/resposta). `orquestrarGeracaoLlm(spec, portas, { orcamento, tempos })` confere o tempo restante antes de cada chamada, passa a cada porta um timeout nunca maior que o restante (e corre contra ele com `comTimeout`), converte estouro em `ErroGeracaoLlm('llm_timeout', etapa)` → **503** `{ erro: 'llm_timeout', etapa, mensagem }` sem nada persistido, e só regenera se couber geração observada + juiz + mínimo; sem orçamento devolve 422 com os motivos da reprovação sem iniciar nova chamada. Saída cortada em `max_tokens` (`truncada`) vira 422 `saida_invalida` sem regenerar. Telemetria `communication_timing` (`d.log`, só números, modelos, etapa e outcome: auth_ms, context_load_ms, generation_ms, deterministic_validation_ms, judge_ms, regeneration_ms, persistence_ms, total_ms). UI: 503 llm_timeout mostra "IA demorou além do limite desta tentativa. Nada foi gravado. Tente novamente." (não é "IA indisponível"); a versão padrão segue disponível. **Live Calibration 01** (primeiro feedback humano de produção, 09/09/2026: a primeira geração real, CEO abaixo do ideal em GET_REFERRAL, abriu com "Cheguei ao seu contato como responsável por essa frente" e pediu indicação de quem responde pela frente — contradição com PASS falso; a comunicação original fica REJECTED como evidência, nunca editada): a "abertura neutra" era uma frase fixa em `specParaLlm` → agora `aberturaNeutra(objetivo)` em `comunicacaoGeracao.ts` (GET_REFERRAL: "Estou tentando chegar à pessoa que conduz essa frente internamente"; demais: "Cheguei ao seu contato ao buscar quem acompanha essa frente"), usada também pelo provedor determinístico; fact gate ganhou o detector contextual `presumeResponsabilidade` (regexes `ATRIBUI_RESPONSABILIDADE`: destinatário apresentado como responsável/líder/dono/condutor da frente; um "quem" entre o pronome e o papel encerra a busca, então "quem responde por essa frente?" nunca bloqueia) — proibido sempre em GET_REFERRAL e, nos demais objetivos, só liberado com indicação real e autorizada (`sourceDisclosure` ALLOWED + `contextoIndicacao`, caso REFERRAL_INTRODUCTION "Fulano me indicou você como responsável"); `PROMPT_JUIZ_V2` explicita o caso; `specParaLlm` envia `restricoesObjetivo` em GET_REFERRAL e `comoCitarSinal` (com claim completo `sinal.oQueAconteceu`, `referenciaSinal` nomeia só a fonte — `referenciaAoSinal` com assunto vazio: "o comunicado da empresa" — e o modelo reformula o claim em frase natural, sem colar o título após "sobre" e sem fatos novos); `CONTENT_SPEC_VERSION` 3. Fixture fictícia do caso real em `comunicacaoCalibracao.test.ts`. **Live Calibration 02 — Relational Fact Binding** (segunda geração real escreveu "nova infraestrutura ... em Cuiabá/MT": o sinal não localiza a infraestrutura; a cidade era a sede da conta — dois fatos válidos, relação não suportada; comunicação preservada como REJECTED): escopo mínimo derivado dos claims sem migration (`escopoDoClaim`: empresa → ACCOUNT_FACT, contato/indicação → CONTACT_FACT, sinal → SIGNAL_FACT, `sinal.local` → SIGNAL_LOCATION, técnico → TECHNICAL_CLAIM); `LeituraSinal.localEvento` opcional (WHERE, em `raw_payload.leitura`) vira claim `sinal.local`; fact gate relacional `localEventoNaoSuportado` em `comunicacaoGeracao.ts`: em frase que fala do sinal/evento (infraestrutura, unidade, fábrica, CD, obra, expansão, projeto, comunicado…), qualquer local (par Cidade/UF ou o local da conta, inteiro ou só a cidade) precisa constar em claim SIGNAL_FACT/SIGNAL_LOCATION, senão "local do evento não suportado pelo sinal: <local>"; "sediada em X" na frase fala da conta e passa; `audiencia.local` sozinho nunca basta. O modelo não recebe mais `audiencia.local` cru: no primeiro contato (GET_REFERRAL/START_DISCOVERY) o local da conta nem vai (nem o claim `empresa.local`); nos demais vai como `localEmpresa { valor, usoPermitido }` e cada claim leva `escopo`; `REGRAS_RELACIONAIS` no payload e `PROMPT_JUIZ_V3` (fatos verdadeiros isolados não criam relação nova: sede ≠ local da obra, data de um sinal ≠ outro evento, investimento ≠ valor de uma obra). Extensões previstas, não implementadas: number/date/project binding. **Approval Path Fix 01** (ao aprovar a primeira comunicação aceitável carregada do Supabase a tela lançava "Cannot read properties of undefined (reading 'length')"): o `content_spec` persistido é o snapshot MÍNIMO por desenho (`contentSpecPersistivel`: deniedClaims sem `texto`, technicalClaims como ids) e o store rodava `validarGeracao` sobre ele como se fosse o ContentSpec completo. Agora: sem edição humana, `transicionarComunicacaoRadar(…, 'APPROVED')` não revalida nada — a geração já passou por fact gate + juiz antes do INSERT — e exige `validacao.ok` (senão bloqueia com mensagem humana), mudando só estado/aprovadoPor/aprovadoEm/ultimoMotivo (o trigger grava exatamente um `radar_communication_event`; snapshot, `generated_content`, `context_hash` e versões seguem imutáveis no banco); com edição humana em produção a tela chama `/api/comunicacao` com `{ acao: 'validar_edicao', communicationId, textoEditado, assuntoEditado? }` (`tratarValidacaoEdicao` em `comunicacaoServidor.ts`: JWT → perfil → comunicação da organização em READY_FOR_REVIEW → Server Truth reconstrói o spec completo → coerência pelo `context_hash` recalculado com as versões gravadas, senão 409 `context_changed` "O contexto comercial mudou desde a geração. Gere uma nova abordagem antes de aprovar." → fact gate → juiz semântico, sem gerar texto e sem gravar) e o store só aceita APPROVED com o `VeredictoEdicao` ok do mesmo rascunho, mesmo `contextHash` e mesmo `hashTextoEfetivo`; sem servidor (modo local) só valida com o spec completo em memória, nunca o snapshot minimizado. `validarGeracao` ganhou defesa estrutural (`problemasEstruturaisDoSpec`/`ehContentSpecCompleto`): estrutura incompatível devolve FAIL "content spec incompleto para validação", nunca TypeError — defesa, não substituto da revalidação. Sem migration; snapshots antigos seguem utilizáveis.
- Funções e alocações de colaboradores (Cadastros › Funções / Alocações, `src/core/equipe.ts`, migration 0043): `FuncaoColaborador` é o catálogo (nome único por organização sem diferenciar caixa/acentos, categoria Fábrica/Canteiro/Escritório, custo/hora padrão, inativar preserva histórico; `salvarFuncao`/`excluirFuncao` exigem `editar_cadastros`). `Colaborador.funcao` segue texto livre (compatibilidade), mas o formulário sugere o catálogo primeiro e preenche o custo/hora padrão quando o campo está zerado. `Alocacao` = colaborador em obra/fábrica/escritório de/até (vazio = em aberto) com percentual 0-1; `salvarAlocacao` (`editar_obra` na obra) exige obra quando local é Obra, datas coerentes e recusa quando a soma dos percentuais do colaborador em qualquer dia do período passa de 100% (`conflitosAlocacao`); `excluirAlocacao` apaga de fato (o diário já apontado não muda). `alocacoesVigentes`/`localDoColaborador` (alocação vigente de maior percentual, senão local/obra padrão do cadastro, com `origem`) e `locaisDoDia` passam a montar o diário do dia pela alocação vigente, caindo no cadastro quando não há nenhuma. A aba Alocações lista vigentes/futuras/encerradas com filtros por colaborador e obra e mostra "onde está cada colaborador hoje".
- **Diretor Financeiro virtual** (`#/diretor`, `src/core/cfo.ts`, tela `src/screens/DiretorFinanceiro.tsx`, função Netlify `/api/diretor-financeiro`): qualquer funcionário conversa ("preciso pagar um frete de R$ 500 amanhã para a Transportadora X, obra Smart Fit") e recebe um parecer DETERMINÍSTICO do motor: `projecaoDiaria` parte do **saldo bancário de hoje pelo extrato da Tesouraria** (`saldoBancarioHoje` = Σ `posicaoBancaria.saldoBancario` das contas ativas, o mesmo número do KPI "Saldo bancário hoje" da Posição diária; nunca da projeção nem de lançamentos não baixados). "Caixa hoje" no parecer, no chat, no briefing e na Central é exatamente esse saldo (`DiaCaixa.saldoInicio` do dia 0); vencidos e vencendo hoje ainda não pagos aparecem como linha à parte (`compromissosHoje` → `saldoAposHoje`), e o piso do caixa é reserva mínima (parâmetro) + reserva vinculada das contas (`reservaVinculadaTotal`), como no "Disponível" da Tesouraria e dali para frente soma só o que ainda não é efetivo — pagamentos vencidos no primeiro dia (têm de ser pagos), previstos por vencimento com o cenário do engine; **recebíveis vencidos ficam fora até entrarem** (`recebiveisVencidos`, citado no parecer) e Realizados não entram (já estão no extrato); `defasagemExtrato` diz até quando o extrato foi importado ("extrato até dd/mm", última transação das contas ativas) e alerta no parecer, no chat, no briefing e na Central quando a defasagem passa de `LIMITE_DEFASAGEM_DIAS` (1 dia) ou quando não há extrato importado e `analisarPagamento` (saldo na data antes/depois, menor saldo dos 30 dias seguintes já com o pagamento, reserva mínima, vencidos, 7 dias, alçada por `etapasExigidas`) → decisão `liberar` (cabe sem tocar na reserva), `reagendar` (primeira data em até 30 dias em que cabe, `dataSugerida`), `atencao` (cabe mas consome a reserva: aval da Diretoria) ou `nao_recomendado` (caixa negativo). A IA só INTERPRETA o texto (valor, data, categoria do plano, obra, fornecedor, intenção) com saída JSON validada contra o catálogo (`interpretacaoDaIa`; categoria/obra fora do catálogo viram nulo; nunca escreve números de caixa); sem chave ou em modo local vale o interpretador determinístico `interpretarPedido` (`extrairValor`/`extrairData`/`extrairObra`/`extrairContraparte`, palavras-chave → categoria). Intenções: pagamento, consulta_caixa (`redigirCaixa`), vencimentos (`redigirVencimentos`), previsoes (`redigirPrevisoes`), ajuda; pedido incompleto pergunta valor/data e `completarPedido` junta a resposta seguinte. "Registrar previsão" cria um lançamento **Rascunho** com `origem = 'diretor-financeiro'` (`actions.registrarPrevisaoDF`, permissão `editar_lancamento`; contraparte "A definir" quando não citada; categoria padrão Outros custos diretos/Outros pagamentos): não entra no caixa oficial nem abre alçada até a decisão. **Dois lados** (`responderDF(ds, usuario, pedido, veCaixa)`): a EQUIPE (quem não tem `ver_bancos`) nunca vê saldo, reserva, vencimentos nem parecer — recebe só o pedido anotado (`redigirParecerEquipe`), "Confirmar e encaminhar à Diretoria", "Ajustar" (valor, data, categoria, obra, para quem) e o andamento em "Meus pedidos" (`statusPedidoDF`: aguardando / validado e programado / na alçada / reagendado / não aprovado com motivo / pago; sugestão no painel e no modo campo quando um pedido seu foi decidido nos últimos 3 dias); pergunta de caixa vira "fica com a Diretoria". A DIRETORIA (`ver_bancos` + `aprovar`: Administrador, Diretoria, Financeiro) abre a **Central** (`centralDF`): briefing falado do dia (pedidos, total, caixa hoje, menor saldo da semana com os pedidos, reserva, minha orientação em contagem, o que vence hoje, alertas), cada pedido já orientado por `orientacaoDF` (programar / reagendar para a primeira data em que cabe / seu aval quando consome a reserva / não recomendado, com saldo na data, menor saldo em 30 dias, reserva e alçada) com um clique que segue a recomendação e sempre "Programar mesmo assim", "Outra data" e "Recusar" (motivo visível ao solicitante), agenda de caixa dos 7 dias com e sem os pedidos e os decididos dos últimos 14 dias; aba "Conversar com o Diretor" com números. `actions.decidirPrevisaoDF(id, 'programar' | 'reagendar' | 'recusar')` — programar vira Programado e passa pelas alçadas normais (pode ficar Pendente), reagendar mantém Rascunho com nova data (rastro nas observações), recusar cancela com motivo; tudo auditado por `salvarLancamento`/`cancelarLancamento`. Chat guardado só na sessão do navegador, por usuário. Nada é pago por ali. Atalho no modo campo. Variáveis: `ANTHROPIC_API_KEY` (painel), `ANTHROPIC_CFO_MODEL` (padrão claude-sonnet-5), timeout 20 s, sem chamada real na suíte. Testes em `src/core/cfo.test.ts`.
- **Radar — Channel Provider 01 (Octadesk Foundation)** (`src/core/radar/canais.ts`, `canaisServidor.ts`, função `/api/channel/octadesk`, painel `src/screens/radar/Entrega.tsx`, auditoria da API em `docs/octadesk.md`, migration 0045): infraestrutura genérica de canal para as comunicações **APROVADAS**. **Nesta fase nada é enviado**: `CommunicationChannelProvider` declara `sendApproved`, mas todo provider chama `recusarEnvio()` (`ENVIO_BLOQUEADO`), e nenhum caminho do sistema faz POST de mensagem (teste garante). Providers: MANUAL e OCTADESK; o core nunca importa a API do Octadesk (o cliente HTTP vive só no módulo server-side, como `vibeServidor`). **Entregabilidade** (`avaliarEntregabilidade`, determinística, recebe um retrato `EstadoProvider`): APPROVED não significa SENDABLE — resultados `SENDABLE_FREEFORM`, `SENDABLE_TEMPLATE`, `MISSING_PHONE`, `NO_APPROVED_TEMPLATE`, `NO_SENDER`, `PROVIDER_NOT_CONFIGURED`, `UNSUPPORTED_CHANNEL`, `NEEDS_REVIEW`; conversa aberta **dentro da janela de 24 h da Meta** (`JANELA_LIVRE_HORAS`) libera mensagem livre, fora dela volta a exigir template aprovado (`NEW_CONVERSATION_TEMPLATE` × `OPEN_CONVERSATION_FREEFORM`). Telefone normalizado em E.164 (`normalizarTelefone`/`whatsappDoContato`) e mascarado em qualquer saída (`mascararTelefone`). **Servidor read-only**: `/api/channel/octadesk` (JWT → perfil real do banco → organização → permissão Radar, o mesmo `autenticar` de `/api/comunicacao`) aceita só `{ acao, comunicacaoId?, contatoId? }` (qualquer outro campo é 400) e chama apenas GET `/auth/check`, `/chat/numbers`, `/chat/templates-message` e `/chat`; o telefone vem do banco (Server Truth), nunca do navegador. Segredos só no Netlify: `OCTADESK_API_KEY`, `OCTADESK_BASE_URL`, `OCTADESK_AGENT_EMAIL` (nunca `VITE_`, nunca em log/resposta); logs só com provider, operação, http_status, latency_ms e ids sanitizados. **Idempotência**: a API do Octadesk **não documenta** chave de idempotência — a garantia é nossa, `chaveIdempotencia` + unique `(organization_id, idempotency_key)` em `radar_communication_delivery`; o cenário "provider aceitou + perdemos a resposta + usuário repete" **ainda não está resolvido**, e por isso o envio segue desligado. **Webhook de entrada não existe na documentação** (o `/chat/external-webhook/...` é o inverso: nós chamamos ele): nenhum endpoint público foi criado; só a interface `ChannelInboundEvent` e a estratégia futura de reconciliação por `GET /chat` + `GET /chat/{id}/messages`, sem polling. `CommunicationTemplateMapping` é só contrato (`MAPEAMENTOS_TEMPLATE` vazio): texto livre do LLM não vira template da Meta automaticamente. UI: bloco ENTREGA na comunicação aprovada com provider, status do canal, entregabilidade, "Verificar Octadesk" e o botão "ENVIO DESATIVADO — PILOTO" inerte. Testes em `canais.test.ts` e `canaisServidor.test.ts` (mocks; zero chamada real). **Send Safety Patch 01** (antes do primeiro envio real): (a) o filtro do lookup é `contact.phoneContacts.number` (`PROPRIEDADE_TELEFONE`, constante única, com regressão); (b) a **janela livre exige prova**: `lastMessageDate` não abre nada sozinho — o servidor lê `GET /chat/{id}/messages` e só libera FREEFORM com mensagem pública do contato dentro de 24 h (`avaliarJanelaLivre`, guardada em `ultimaMensagemInboundEm`/`janelaLivreAte`/`janelaComprovada`); a direção vem **só** de `sentBy.type`, nunca do `status` (`received` = "delivered to recipient", mensagem nossa), e direção desconhecida nunca prova; sem prova, volta para TEMPLATE ou NEEDS_REVIEW; (c) **máquina de estados** `READY → REQUESTED → ACCEPTED/FAILED/UNKNOWN → DELIVERED/FAILED/UNKNOWN`, terminais sem regressão, espelhada em `TRANSICOES_ENTREGA` e no trigger `radar_delivery_estado` (única porta de mudança de status, que produz o evento); (d) `reconcileDelivery` devolve FOUND/NOT_FOUND/AMBIGUOUS comparando a **impressão** (hash canônico do corpo, calculado em trânsito, nunca gravado em claro; `impressaoEnvio` para o ledger, `impressaoComparavel` para a comparação); AMBIGUOUS e UNKNOWN **nunca** autorizam reenvio automático (`permiteReenvioAutomatico`). **Delivery Authority & Reconciliation Patch 01**: (a) `authenticated` perdeu INSERT/UPDATE no ledger (só SELECT, aqui e na trilha de eventos); criar e transicionar entrega só pelas funções server-only `radar_delivery_create`/`radar_delivery_transition` (EXECUTE apenas para `service_role`), chamadas depois de JWT → perfil → organização → permissão Radar, com teste varrendo `src/` para o navegador nunca escrever nem chamar as RPCs; (b) **comando imutável** depois do INSERT (organização, comunicação, empresa, contato, provider, canal, modo, chave de idempotência, fingerprint, remetente, template, quem pediu e criação; `requested_at` é preenchido uma vez na ida para REQUESTED e congela) — mudar comando é criar outra entrega, e só os campos de resultado do provider evoluem pela porta controlada; (c) **ator real**: o evento distingue quem pediu (`requested_by`) de quem transicionou (`actor_id` + `actor_kind` USER/SERVER/PROVIDER/SYSTEM), sempre do JWT validado no servidor; (d) **reconciliação Server Truth**: o navegador manda só o `deliveryId` e o servidor reconstrói entrega, comunicação, texto aprovado efetivo, contato e dados do provider; com id de conversa desconhecido (send-template com resposta perdida) todas as conversas do telefone viram candidatas e a impressão esperada é recalculada **com o id de cada candidata** (nunca `hash(null + texto)`); (e) **dois fingerprints separados**: `impressaoComando` (request_fingerprint, auditável, no ledger) e `impressaoMensagem` (message_match_fingerprint, transitória, exige conversaId); (f) `classificarFalhaEnvio`: só rejeição explícita e definitiva vira FAILED; timeout, rede, 5xx e resposta ininteligível viram UNKNOWN. **Send Canary 01** (caminho real de envio, fechado por três chaves): `OCTADESK_SEND_MODE` (`disabled` padrão, `canary`, `pilot` ainda sem nenhum destino liberado) + allowlist `OCTADESK_CANARY_NUMBERS` (só no servidor, nunca devolvida ao navegador; destino fora dela é 403 `canary_destination_not_allowed`) + entregabilidade apta. Ações `preparar_envio` (devolve modo, coerência de canal, entregabilidade, números e templates aprovados atuais, e se o destino está autorizado) e `enviar_canary` em `/api/channel/octadesk`; o navegador manda só `communicationId` e, no máximo, `senderId`/`templateId` escolhidos entre as opções devolvidas e **revalidados** contra a lista viva. **Delivery first**: `radar_delivery_create` → READY, `radar_delivery_transition` READY → REQUESTED e só então o POST; falhou antes disso, nada é enviado; entrega que já existia (duplo clique) nunca gera segundo POST. FREEFORM usa `POST /chat/{id}/messages` e exige janela comprovada; TEMPLATE usa `POST /chat/send-template` com template approved+enable da lista viva e mapeamento explícito (`OCTADESK_TEMPLATE_MAPPINGS`) quando há variáveis — o texto do LLM nunca vira template automaticamente. Retorno: ids presentes → ACCEPTED (só ids e status do provider, sem payload bruto), `errorCode` → FAILED, timeout/rede/5xx/2xx sem ids → UNKNOWN, e aí a UI mostra "Reconciliar entrega", nunca "tentar de novo". A comunicação **não** vira SENT nesta fase. Testes em `canaisEnvio.test.ts` (mocks; cada caso confere se houve ou não POST). `PLAYBOOK_VERSION` subiu para 1.2: REFERRAL_INTRODUCTION não cita mais a abertura antiga que presumia responsabilidade do contato (`playbookHigiene.test.ts` impede a regressão).
- Pedidos de compra (`#/compras`, `PedidoCompra`, tabelas `purchase_order`/`purchase_order_item`, migration 0024): rascunho editável; **Emitir** gera um lançamento previsto (Programado, categoria do pedido, serviço, faturamento direto, vencimento = data + prazo) passando pelas alçadas normais, e congela os itens; **Receber** atualiza quantidades e, se marcado, grava o preço pago como preço vigente do insumo (`precoFonte` = pedido/fornecedor); **Cancelar** cancela o lançamento junto (se não realizado). Comparativo orçado × comprado usa a explosão de insumos (`curvaInsumos`) dos orçamentos Contratados da obra contra os itens de pedidos ativos com insumo vinculado.
- Data-base automática (`auto_base_date`) avança para hoje; a data do saldo de abertura de cada conta é independente dela.
- Orçamentos (`#/orcamentos`): catálogo de insumos e composições (origem SINAPI, TCPO ou Própria; casamento por origem + código na importação), custo unitário = Σ coeficiente × preço (recursivo, sem ciclos), preço de venda = custo × (1 + BDI), curva ABC com cortes 80%/95%. Item pode ter preço de venda informado (`precoUnitarioVenda`, proposta/contrato): substitui custo × (1 + BDI) e a margem passa a ser calculada contra o custo das composições; `porServico` agrega itens vinculados a serviços da obra. "Contratar" gera um serviço por item (custo orçado = custo direto, preço de venda = com BDI ou redistribuído ao valor do contrato), grava `obra.custoOrcado` e congela os itens. A base SINAPI é pública (Caixa); a TCPO é licenciada e não pode ser embutida: só importar o que o usuário exportar.

## Estado e decisões (atualizar ao mudar)

- Obra ativa: OB-SF-CL-01 Smart Fit César Lattes; contrato global 4,1 mi com 65% faturado direto pelo cliente; receita líquida EIFF ≈ 1,285 mi; 26 eventos de medição; E03/E05/E06/E07 faturados na NF 47. Mês contratual 1 = junho/2026 (a confirmar).
- Usuário único em validação: `augusto@eiff.com.br`, papel Administrador (voltar a Diretoria quando a equipe entrar). Radar em produção: lote piloto carregado em 08/09/2026 (91 empresas e 17 decisores do Vibe, fonte VIBE, jobs com o nome dos datasets); Grupo Sinova e GRUPO SINAGRO ficaram como possível duplicata pendente (mesmo domínio, business_id diferente). Signal Pilot 01 acompanha 10 dessas empresas; próximo passo é inserir sinais reais à mão. Nenhuma prospecção nova nem consumo de créditos sem ordem explícita.
- Migrations aplicadas até 0048 (0032: decisores do Radar; 0033: `explorium_business_id` na empresa e `source_external_id` no contato para o Vibe; 0034: ledger `radar_vibe_operation`, política `radar_vibe_credit_policy` e RPCs `reserve_vibe_operation`/`update_vibe_operation`/`vibe_operation_state`/`vibe_budget_status`; 0035: RPCs mutáveis server-only com `p_user_id`, `search_path` fixo, política editável só por Administrador; 0036: coluna `version` em `radar_contact` e `radar_project`, exigida pelo trigger `touch_updated_at` — sem ela todo UPDATE nessas tabelas falhava; 0037: `deleted_at`/`deleted_by`/`deletion_reason` em `financial_entry` para a exclusão lógica de lançamentos; 0038: `radar_communication` e `radar_communication_event`; 0039: evento imutável (só SELECT para authenticated, triggers recusam update/delete, insert só pelo trigger definer), snapshot imutável (trigger recusa mudar contexto/conteúdo gerado/versões/criação; só campos operacionais mudam) e minimização recursiva de PII/raw por `jsonb_tem_chave_profunda` em content_spec, generated_content e edited_content; 0040: trigger de coerência referencial de `radar_communication` — contato e sinal da mesma empresa, empresa/contato/sinal/estratégia da mesma organização; 0041: `field_photo`, fotos de campo com a imagem comprimida na linha, RLS por organização/obra, exclusão pelo autor ou por quem edita a obra; 0042: bucket privado `fotos-campo` no Storage com políticas por prefixo da organização, `field_photo.storage_path`, `data_url` opcional com CHECK "caminho ou imagem"; 0043: `job_function` (catálogo de funções) e `worker_allocation` (alocações por período), RLS por organização, escrita para Administrador/Diretoria/Financeiro/Gestor de obra (+ Engenharia nas alocações), triggers touch/audit; 0044: `parameter_set.statement_cutoff` = 2026-09-01, corte do extrato; 0045: `radar_communication_delivery`, ledger de entrega das comunicações com idempotência própria, coerência referencial e minimização de PII; 0046: `radar_communication_delivery_event` append-only e o trigger `radar_delivery_estado`, máquina de estados da entrega no banco; 0047: autoridade server-only do ledger — `authenticated` só SELECT, RPCs `radar_delivery_create`/`radar_delivery_transition` só para `service_role`, comando da entrega imutável e ator real da transição; 0048: coerência de canal dentro de `radar_delivery_create` — entrega tem de ter o mesmo canal da comunicação e Octadesk só entrega WhatsApp).
- Vibe Prospecting (`docs/vibe.md`): função Netlify `/api/vibe` (`netlify/functions/vibe.ts`, papel Administrador/Diretoria, chave `VIBE_API_KEY` só no painel do Netlify), aba "Vibe Prospecting" no Command Center (`src/screens/radar/Vibe.tsx`), regras puras em `src/core/radar/vibe.ts` (prioridade de decisores, `classificarPool` por decision fit, estimativa de créditos, prospect → contato) e `src/core/radar/vibeServidor.ts` (hash da requisição, decisão de reserva espelhando a RPC, tamanho de página pelo teto, paginação, catálogo `POOL_JOB_LEVEL`/`POOL_JOB_DEPARTMENT`, cache de e-mail, force refresh); **toda ação paga exige operação reservada no banco** (simular → reservar → executar com `operationId` + `idempotencyKey`; budget/reserve do navegador não são fonte de verdade); as RPCs `reserve_vibe_operation`/`update_vibe_operation` são server-only: a função Netlify valida o JWT e só então as chama com `SUPABASE_SERVICE_ROLE_KEY` (só no painel do Netlify, nunca `VITE_`, nunca no navegador ou em logs), passando o saldo consultado na Explorium pelo servidor; nenhum código React chama essas RPCs; script alternativo `scripts/vibe.mjs` com `.env.local` (`validar-filtros` obrigatório antes de `decisores --executar`); MCP remoto em `.mcp.json` (OAuth). Nunca colar a chave no chat nem em arquivos versionados. Busca de decisor por conta (bloco 3b, `buscarDecisor` em `src/core/radar/buscaDecisor.ts`): preview de até 10 candidatos por conta sem créditos, ranking por decision fit → adequação funcional → qualidade de dados, comparação com o contato atual e recomendação ENRICH/KEEP_CURRENT/RESEARCH_MORE; não importa nem enriquece. Decision Maker Calibration 01: senioridade do prospect = job_level válido, senão inferida do título pela regra do Radar (Outro = indefinida); bônus de departamento do decision fit usa a chave canônica (Operations→operacoes, Engineering→engenharia, Procurement→compras, manufacturing→producao…; raw preservado; Trades sem bônus); liderança industrial explícita no cargo (gerente/coordenador/supervisor industrial, plant manager…) é persona MANUFACTURING com a senioridade real, nunca Diretor industrial; as personas *_DIRECTOR embutem nível (conflação conhecida, não alterada). Regras de persona vivem em `radar_persona_rule` em produção: alinhar com `npx vite-node scripts/radar-persona-alinhar-producao.mts -- --perfil <uuid> [--executar]` (simula, mostra o diff, só aceita diferenças aprovadas em `personaRegras.ts`, grava numa transação, recalcula contatos e empresas afetadas; o teste `personaRegras.test.ts` prende padrões e banco). Alinhado em 08/09/2026: 17 regras, 3 contatos recalculados (Cristiano Camargo 65→71 decisor), Locks 21,8→25,5. Orçamento `ORC-328` (proposta Modo 328 R02, 05/05/2026, R$ 4.131.354,17, 81 itens em 14 etapas) carregado como Contratado na OB-SF-CL-01 com preço de venda por item (`sale_unit_price` = total do PDF ÷ quantidade) e cada item vinculado ao serviço SFCL-xx cujo `budget_base` é o total da etapa (impermeabilização foi para SFCL-05). Fonte: `scripts/proposta-smartfit.mjs` → `0018_smartfit_proposta.sql` (PDF vetorial sem texto, transcrito de páginas renderizadas). Catálogo SINAPI GO 07/2026 não desonerado carregado (migration 0019 gerada por `scripts/importar-sinapi.mjs`: todos os 6.120 insumos com preço de GO ou atribuído de SP, 2.222 composições dos grupos relevantes + dependências, 13,5 mil itens; leitor validado contra o custo oficial da aba CSD em 95% das composições, restante é arredondamento de centavos). 60 dos 81 itens da proposta vinculados a composições/insumos SINAPI (`VINCULOS` em `scripts/proposta-smartfit.mjs`, migration 0020; insumos usados direto viram composições próprias `INS-<código>`). Sem equivalente SINAPI: estrutura metálica MODO (10.3), isopainel PIR, pinos stud, chumbadores, parafusos, verbas de instalações, transporte, EPI, limpeza final. Os 21 itens restantes usam composições próprias da EIFF (`scripts/composicoes-eiff.mjs` → migration 0022; 0021 trouxe do SINAPI as composições de mão de obra/equipamento necessárias): estrutura metálica por kg = `EIFF-FAB-KG` (fabricação, 17,5 HH/t, aço 1,05 kg/kg) + `EIFF-MON-KG` (montagem, 26 HH/t, guindaste 1,2 h/t) somadas em `EIFF-EST-KG`; pinos stud, parafusos, chumbadores, isopainel, cumeeira, rufo 80 cm, transporte, canteiro e subempreitadas (85% do preço de venda). Todos os insumos próprios (`EIFF-INS-xx`) e coeficientes são ESTIMATIVAS marcadas em notes/price_source, a substituir por cotação ou apontamento real. Com isso os 81 itens têm custo: margem prevista da proposta ≈ 22% (ver `node scripts/composicoes-eiff.mjs` para a prévia por composição).
- Pendências conhecidas: registrar E01/E02/E04/E08 se medidos; lançar custos com serviço; alçadas reais (DEC-03); reserva mínima (DEC-09); plano Pro do Supabase para backup.
- Documentação: `README.md`, `docs/arquitetura-e-regras.md`, `docs/implantacao-supabase.md`, `docs/publicacao-automatica.md`, `docs/assistente.md`, `docs/radar.md`.

## Trabalho em paralelo (duas máquinas)

Sempre `git pull` ao começar e `git push` ao terminar; commits pequenos com mensagem em português. Antes de criar uma migration,
conferir o último número no repositório e no banco (`select * from audit_log order by id desc limit 1` não serve; use a lista de arquivos).
Não editar `seed.json` à mão: regenerar com `npm run migrate:planilha`.
