node_modules
dist
.env
.env.*
!.env.example
*.log
.DS_Store
eiff-control-site.zip

# Local Netlify folder
.netlify

# Integracao Vibe Prospecting: chave so em .env.local; exportacoes com dados pessoais fora do repositorio
.env.local
dados/vibe/

# worktrees de agentes (locais)
.claude/worktrees/

# Snapshot do CNO (Dados Abertos da Receita): ~315 MiB comprimidos, ~1,4 GiB de CSV. Nunca versionar.
# As fixtures do LE-3A sao sinteticas e vivem dentro do proprio teste.
cno.zip
cno.zip.parcial
cno*.csv
cno-metadados.pdf
dados/cno/

# SQL de rehearsal/carga gerado pelos runners de producao (contem payloads reais). Nunca versionar.
scratch/
